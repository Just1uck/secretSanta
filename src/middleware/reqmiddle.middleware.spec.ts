import { ReqmiddleMiddleware } from './reqmiddle.middleware';

describe('ReqmiddleMiddleware', () => {
  it('should be defined', () => {
    expect(new ReqmiddleMiddleware()).toBeDefined();
  });
});
